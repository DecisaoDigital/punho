# v0.0.9 — hotfix login/reset password

## Fixes
- **Login: gestor de passwords do Android** agora funciona (autofillHints + AutofillGroup + finishAutofillContext).
- **Login: olhinho** para mostrar/esconder a palavra-passe.
- **Reset password: link do email** agora vai para `punho://auth/callback` (era `washinvoice://`, esquema errado do POS — link caía em página em branco).
- **Registo: autofillHints** — Android oferece guardar credenciais logo à primeira.

## Bugs conhecidos
- 1c smoke v0.0.8: `autofocus` está no código em todos os passos do OnboardingPage; se o teclado ainda não abrir, é comportamento OEM (algumas ROMs Android bloqueiam autofocus após PageView). Investigar em v0.0.10.
