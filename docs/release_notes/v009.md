# v0.0.9 — hotfix login/reset password + cursor do onboarding

## Fixes
- **1c Onboarding: cursor não aparecia no campo após avançar de passo.** `autofocus: true` só dispara na 1ª montagem do FocusScope; o cursor ficava sem casa e o teclado abria "solto" (o que escrevias não ia a lado nenhum). Fix: `FocusNode` partilhado com `requestFocus` pós-frame sempre que o step muda.
- **2a Login: gestor de passwords do Android** agora funciona (autofillHints + AutofillGroup + finishAutofillContext).
- **2c Reset password: link do email** vai para `punho://auth/callback` (era `washinvoice://`, esquema errado do POS — caía em página em branco).
- **2d Login: olhinho** para mostrar/esconder a palavra-passe.
- **Registo: autofillHints** — Android oferece guardar credenciais logo à primeira.
